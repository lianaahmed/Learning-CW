%
%
function task1_1(X, Y)
% Input:
%  X : M-by-D data matrix (double)
%  Y : M-by-1 label vector (unit8)
 
% indices stores all the indices we need for X
% by ga
indices = find(Y);
imageArray = [];

for i = l:length(indices)
    
    imageArray = cat(3, imageArray, X(i));
    


end
